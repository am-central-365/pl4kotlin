package com.amcentral365.pl4kotlin.integrationTests

import org.junit.Test

class postfixITCase {

    @Test
    fun `with postfix ITCase`() {
        println("Integration test with postfix ITCase")

        //for(p in System.getProperties())
        //    println("  ${p.key} : ${p.value}")
    }
}
